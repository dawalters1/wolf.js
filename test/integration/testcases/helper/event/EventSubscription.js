import initialiseClient from '../../../utility/initialiseClient.js';

let client;
describe('EventSubscription Helper', () => {
  before(async function () {
    client = await initialiseClient();
  });

  describe('fetch', () => {

  });

  describe('add', () => {

  });

  describe('remove', () => {

  });
});
